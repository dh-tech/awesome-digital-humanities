name: awesome-lint

on: [push, pull_request]

permissions:
  contents: read

jobs:
  Awesome_Lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - run: npx awesome-lint
